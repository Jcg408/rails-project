    <%= form_for new_staff_service_path do |f| %>
   
  <%= f.label :staff, "Staff Name" %>
  <%= f.collection_select :staff, Staff.all, :id, :name %> <br> 

  <%= f.label :service, "Spa Service" %>
  <%= f.collection_select :service, Service.all, :id, :name %> <br> 

  <%= f.submit %>
  <%end%>